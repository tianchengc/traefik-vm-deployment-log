entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    exposedByDefault: false
  file:
    filename: /etc/traefik/dynamic.yml

certificatesResolvers:
  letsencrypt:
    acme:
      email: tiancheng.chen.workspace@gmail.com
      storage: /etc/traefik/acme.json
      httpChallenge:
        entryPoint: web